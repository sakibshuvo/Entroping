openapi: 3.0.3
info:
  title: tool-api-demo
  version: 1.0.0
paths:
  /tools/list:
    get:
      operationId: listTools
      responses:
        "200":
          description: List available tools.
  /tools/call:
    post:
      operationId: callTool
      responses:
        "200":
          description: Execute tool.
